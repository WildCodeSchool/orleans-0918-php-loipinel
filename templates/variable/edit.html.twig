{% extends 'layout.html.twig' %}

{% form_theme form 'bootstrap_4_horizontal_layout.html.twig' %}

{% block body %}
    <h1 class="variablesTitle bg-primary">Editer les variables</h1>

    <a href="{{ path('variable_index') }}" class="backToList"><i class="fas fa-arrow-left"></i> Revenir à la liste</a>

    <div class="container text-center">
        {{ include('variable/_form.html.twig', {'button_label': 'Valider vos modifications'}) }}
    </div>

{% endblock %}
