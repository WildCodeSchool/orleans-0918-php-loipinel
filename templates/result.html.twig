{% extends 'layoutPdf.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row mb-5 bg-primary text-center">
            <div class="col">
                <h1 class="pdfResult">Résultat de la simulation</h1>
            </div>
        </div>
    </div>

    {{ include('resultTable.html.twig') }}

    <div class="row pdf justify-content-center">
        <a href="{{ path('pdf_export') }}" class="btn btn-primary btn-lg">Exporter en pdf</a>
    </div>
{% endblock %}