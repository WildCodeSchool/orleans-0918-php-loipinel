{% extends 'layout.html.twig' %}

{% block body %}

    <div class="container-fluid">
        <div class="row mb-5 result text-center">
            <div class="col">
                <h1 class="pdfResult">Résultat de la simulation</h1>
            </div>
        </div>

        <div class="previous">
            <a href="{{ path('civilStatus_show') }}"><i class="fas fa-arrow-left"></i> Retour au formulaire</a>
        </div>

        <div class="row mb-5">
            {{ include('resultTable.html.twig') }}
        </div>

        <div class="row pdf justify-content-center">
            <a href="{{ path('pdf_export') }}" class="btn btn-primary btn-lg">Exporter en pdf</a>
        </div>

    </div>

{% endblock %}
